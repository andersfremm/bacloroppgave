# *******************************
# Definition file for counter_ip
# *******************************

# Defining type and regbank instance for the IP
group -n counter_ip -t mux -i i0rb_counter

# defining base address, sync/async clock and retiming at bus interface
inst -n i0rb_counter -sg counter -ba 0 -sy 0 -rtc 0 -rtb 0

# souring seperate files for register definition and GUI
inst -n i0rb_counter  -f ./counter_reg.txt
inst -n i0rb_counter  -f ./counter_gui.txt
